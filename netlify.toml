# SafeWatch — Netlify
# Form gönderimi: netlify/functions/send-feedback.ts (Resend)
# Forms paneli kullanılmaz. Function yolu: /send-feedback

[build]
  command = "npm run build"

[build.environment]
  NODE_VERSION = "20"

[functions]
  node_bundler = "esbuild"
