# SafeWatch — Netlify
# Form gönderimi: netlify/functions/send-feedback.ts (Resend)
# Forms paneli kullanılmaz.

[build]
  command = "npm run build"

[build.environment]
  NODE_VERSION = "20"

[functions]
  directory = "netlify/functions"
